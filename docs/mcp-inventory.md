# MCP Tool Inventory

**Total:** 333 tools across 53 modules (262 core + 71 bundle)

Generated by `scripts/gen_mcp_inventory.py` from `@mcp.tool()` decorators. Do not edit by hand — re-run the script when tools are added or removed. Other docs (README, `platform-overview.md`, `mcp-server.md`, `CLAUDE.md`) link here as the single source of truth for the tool count.

---

## Core (262 tools, 41 modules)

### `roost/mcp/tools_activity.py` — 4 tools

| Tool | Description |
|---|---|
| `get_active_task` | Return the current active task (id, title, status, project) or null if unset. |
| `get_task_activity` | Return the activity trail for a task — actions taken, tools used, artifacts produced. |
| `log_activity` | Log an activity entry against a task. |
| `set_active_task` | Set the active task for the current work session. |

### `roost/mcp/tools_audit.py` — 2 tools

| Tool | Description |
|---|---|
| `audit_for_entity` | Return the audit trail for a specific entity (cadence_draft, lead, etc.). |
| `audit_recent` | Return the newest audit-log rows from the system-of-record trail. |

### `roost/mcp/tools_bundles.py` — 3 tools

| Tool | Description |
|---|---|
| `morning_briefing` | Assemble a morning context briefing: today's calendar, in-progress |
| `prep_for` | Prepare context for a meeting or interaction with a person: contact |
| `project_pulse` | Get a status snapshot for a project: tasks by status, upcoming events, |

### `roost/mcp/tools_calendar.py` — 2 tools

| Tool | Description |
|---|---|
| `get_today_events` | Get all calendar events for today. |
| `get_week_events` | Get calendar events for the next N days. |

### `roost/mcp/tools_calendar_write.py` — 5 tools

| Tool | Description |
|---|---|
| `calendar_create_event` | Create a new Google Calendar event. |
| `calendar_delete_event` | Delete a Google Calendar event. |
| `calendar_list_calendars` | List all Google Calendars the user has access to. |
| `calendar_search_events` | Search calendar events by text query. |
| `calendar_update_event` | Update an existing Google Calendar event. |

### `roost/mcp/tools_comms.py` — 5 tools

| Tool | Description |
|---|---|
| `delete_communication` | Remove a communication record. |
| `get_contact_history` | Get the communication timeline for a contact. |
| `log_communication` | Log a communication with a contact (call, note, whatsapp, etc.). |
| `sync_contact_emails` | Pull Gmail history for a contact and store as communication records. |
| `sync_contact_meetings` | Match calendar events to a contact by name and store as meetings. |

### `roost/mcp/tools_contacts.py` — 13 tools

| Tool | Description |
|---|---|
| `add_contact_to_entity` | Link a contact to an entity (organisation). Upserts if already linked. |
| `create_contact` | Create a new contact. |
| `find_contact_by_identifier` | Find a contact by an identifier (e.g. find by microsoft user ID, telegram ID, etc.). |
| `get_contact` | Get a specific contact by ID with their affiliations. |
| `harvest_email_contacts` | Scan Gmail for contacts and optionally create them in the contacts database. |
| `import_google_contacts` | Import contacts from Google Contacts (People API) into the contacts database. |
| `list_contact_entities` | List contact-entity affiliations (who works where). |
| `list_contacts` | List all contacts, optionally filtered by entity. |
| `remove_contact_from_entity` | Remove a contact-entity affiliation by its ID. |
| `remove_contact_identifier` | Remove a contact identifier by its ID. |
| `search_contacts` | Search for a contact by name (partial, case-insensitive match). |
| `set_contact_identifier` | Add or update an identifier for a contact (email, phone, microsoft, telegram, linkedin, etc.). |
| `update_contact` | Update an existing contact. |

### `roost/mcp/tools_docgen.py` — 2 tools

| Tool | Description |
|---|---|
| `convert_and_upload` | Convert a document and upload to Google Drive in one step. |
| `convert_document` | Convert a document between formats using pandoc. |

### `roost/mcp/tools_docker.py` — 6 tools

| Tool | Description |
|---|---|
| `docker_compose_down` | Stop Docker Compose services on a remote server. |
| `docker_compose_ps` | List Docker Compose service status on a remote server. |
| `docker_compose_up` | Start Docker Compose services on a remote server. |
| `docker_logs` | View logs from a Docker container on a remote server. |
| `docker_ps` | List Docker containers on a remote server. |
| `docker_pull` | Pull a Docker image on a remote server. |

### `roost/mcp/tools_docs.py` — 5 tools

| Tool | Description |
|---|---|
| `gdocs_append_text` | Append text to the end of a Google Doc. |
| `gdocs_create_from_template` | Create a new Google Doc by copying a template and filling placeholders. |
| `gdocs_insert_image` | Insert an image at the end of a Google Doc. |
| `gdocs_read_content` | Read the full text content of a Google Doc. |
| `gdocs_replace_text` | Replace placeholder text throughout a Google Doc. |

### `roost/mcp/tools_drive.py` — 4 tools

| Tool | Description |
|---|---|
| `drive_download` | Download a file or folder from Google Drive to the local filesystem. |
| `drive_list` | List files and folders in a Google Drive path. |
| `drive_search` | Search for files on Google Drive by filename pattern. |
| `drive_upload` | Upload a local file or directory to Google Drive. |

### `roost/mcp/tools_entities.py` — 2 tools

| Tool | Description |
|---|---|
| `get_entity` | Get an entity with its associated projects and people. |
| `list_entities` | List all entities (companies/organisations). |

### `roost/mcp/tools_gemini.py` — 10 tools

| Tool | Description |
|---|---|
| `gemini_agent` | Run a multi-step Gemini agent with file access tools. |
| `gemini_clean_text` | Clean or fix text using Gemini — OCR correction, formatting, deduplication. |
| `gemini_compare` | Compare two texts using Gemini — diff, merge analysis, cross-reference. |
| `gemini_generate` | Single-shot Gemini generation with full control over context and output. |
| `gemini_image` | Generate images using Gemini or Imagen models. |
| `gemini_process_document` | Read a local file and process it with Gemini. |
| `gemini_research` | Research a topic using Gemini with Google Search grounding. |
| `gemini_summarize` | Summarize or analyze large text using Gemini's large context window. |
| `gemini_usage` | Query Gemini API usage and estimated costs. |
| `gemini_vision` | Analyze one or more images with Gemini's vision capabilities. |

### `roost/mcp/tools_gmail.py` — 7 tools

| Tool | Description |
|---|---|
| `download_attachments` | Download attachments from a Gmail message to a local directory. |
| `list_attachments` | List attachments on a Gmail message. |
| `list_google_accounts` | List all connected Google accounts. |
| `list_labels` | List all Gmail labels (system and user-created). |
| `read_thread` | Read all messages in an email thread. |
| `search_emails` | Search Gmail messages using Gmail query syntax. |
| `send_email` | Send an email via Gmail. |

### `roost/mcp/tools_guardian.py` — 13 tools

| Tool | Description |
|---|---|
| `approve_learned_skill` | Approve a pending learned skill — it will be included in agent prompts. |
| `get_autonomy_level` | Get the current autonomy level (supervised/assisted/autonomous). |
| `get_background_run_result` | Get the result of a background agent run. |
| `get_guardian_log` | Get recent Guardian AI decisions (warn/block events). |
| `get_guardian_stats` | Get Guardian AI summary stats — total decisions, blocks, top blocked rules. |
| `get_usage_history` | Get daily AI token usage for the last N days. |
| `get_usage_today` | Get today's AI token usage — input/output tokens, estimated cost, run count. |
| `list_background_runs` | List background agent runs. |
| `list_checkpoints` | List recent agent action checkpoints (for rollback). |
| `list_learned_skills` | List learned skills (self-improving agent patterns). |
| `reject_learned_skill` | Reject a pending learned skill — it won't be used by the agent. |
| `rollback_checkpoint` | Rollback a checkpoint — undo a previous agent action. |
| `spawn_background_agent` | Spawn a background agent to perform a task asynchronously. |

### `roost/mcp/tools_k8s.py` — 5 tools

| Tool | Description |
|---|---|
| `kubectl_apply` | Apply a Kubernetes manifest on a remote server. |
| `kubectl_delete` | Delete a Kubernetes resource on a remote server. |
| `kubectl_describe` | Describe a Kubernetes resource on a remote server. |
| `kubectl_get` | Get Kubernetes resources on a remote server. |
| `kubectl_logs` | View logs from a Kubernetes pod on a remote server. |

### `roost/mcp/tools_meeting_notes.py` — 3 tools

| Tool | Description |
|---|---|
| `generate_presentation` | Generate a PowerPoint presentation (.pptx) using Gemini AI. |
| `structure_meeting_notes` | Structure raw meeting notes into key points, action items, and decisions. |
| `transcribe_audio` | Transcribe an audio file using Gemini multimodal AI. |

### `roost/mcp/tools_memory.py` — 4 tools

| Tool | Description |
|---|---|
| `forget` | Delete a specific memory entry. |
| `pin_memory` | Pin or unpin a memory entry. Pinned entries don't auto-expire. |
| `recall` | Recall cross-channel memory entries. |
| `remember` | Store something in cross-channel memory. |

### `roost/mcp/tools_ms_calendar.py` — 7 tools

| Tool | Description |
|---|---|
| `ms_calendar_create_event` | Create a new Microsoft Calendar event. |
| `ms_calendar_delete_event` | Delete a Microsoft Calendar event. |
| `ms_calendar_list_calendars` | List all Microsoft Calendars the user has access to. |
| `ms_calendar_search_events` | Search Microsoft Calendar events by text query. |
| `ms_calendar_update_event` | Update an existing Microsoft Calendar event. |
| `ms_get_today_events` | Get all Microsoft Calendar events for today. |
| `ms_get_week_events` | Get Microsoft Calendar events for the next N days. |

### `roost/mcp/tools_ms_email.py` — 4 tools

| Tool | Description |
|---|---|
| `ms_list_folders` | List all Outlook mail folders with unread/total counts. |
| `ms_read_conversation` | Read all messages in an Outlook conversation thread. |
| `ms_search_emails` | Search Outlook emails via Microsoft Graph. |
| `ms_send_email` | Send an email via Microsoft Outlook. |

### `roost/mcp/tools_ms_excel.py` — 3 tools

| Tool | Description |
|---|---|
| `ms_excel_list_worksheets` | List worksheets in an Excel workbook stored in OneDrive. |
| `ms_excel_read_range` | Read a range of cells from an Excel workbook in OneDrive. |
| `ms_excel_write_range` | Write values to a range of cells in an Excel workbook in OneDrive. |

### `roost/mcp/tools_ms_onedrive.py` — 4 tools

| Tool | Description |
|---|---|
| `ms_onedrive_download` | Download a file from OneDrive to the local filesystem. |
| `ms_onedrive_list` | List files and folders in a OneDrive path. |
| `ms_onedrive_search` | Search OneDrive for files by name or content. |
| `ms_onedrive_upload` | Upload a local file to OneDrive. Auto-selects simple (<4MB) or resumable (up to 250MB) upload. |

### `roost/mcp/tools_ms_sharepoint.py` — 4 tools

| Tool | Description |
|---|---|
| `ms_sharepoint_download` | Download a file from a SharePoint site's document library. |
| `ms_sharepoint_list_files` | List files and folders in a SharePoint site's document library. |
| `ms_sharepoint_list_sites` | List or search SharePoint sites. |
| `ms_sharepoint_upload` | Upload a local file to a SharePoint site's document library (simple upload, <4MB). |

### `roost/mcp/tools_ms_teams.py` — 16 tools

| Tool | Description |
|---|---|
| `ms_teams_add_reaction` | Add a reaction to a Teams chat message. |
| `ms_teams_create_chat` | Create a new Teams chat (1:1 or group). |
| `ms_teams_download_channel_file` | Download a file from a Teams channel's SharePoint folder. |
| `ms_teams_download_images` | Download all images and file attachments from a Teams chat. |
| `ms_teams_list_channel_files` | List files in a Teams channel's SharePoint folder. |
| `ms_teams_list_channels` | List channels in a Microsoft Team. |
| `ms_teams_list_chat_members` | List members of a Teams chat with their user IDs and emails. |
| `ms_teams_list_chats` | List the user's recent Teams chats (1:1 and group). |
| `ms_teams_list_teams` | List all Microsoft Teams the user has joined. |
| `ms_teams_lookup_user` | Look up a Microsoft 365 user by email address. Returns their Azure AD user ID. |
| `ms_teams_read_chat` | Read recent messages from a Teams chat. |
| `ms_teams_read_messages` | Read recent messages from a Teams channel. |
| `ms_teams_remove_reaction` | Remove a reaction from a Teams chat message. |
| `ms_teams_reply_channel_message` | Reply to a message in a Teams channel (threaded reply). |
| `ms_teams_send_chat` | Send a message in a Teams chat. |
| `ms_teams_send_message` | Send a message to a Teams channel. |

### `roost/mcp/tools_notes.py` — 4 tools

| Tool | Description |
|---|---|
| `create_note` | Create a quick note. |
| `delete_note` | Delete a note by ID. |
| `get_note` | Get a single note by ID. |
| `list_notes` | List recent notes with optional filters. |

### `roost/mcp/tools_notion.py` — 16 tools

| Tool | Description |
|---|---|
| `notion_append_blocks` | Append content blocks to a page or block. |
| `notion_archive_page` | Archive (soft-delete) a Notion page. |
| `notion_create_comment` | Add a comment to a Notion page. |
| `notion_create_database` | Create a new Notion database as a child of a page. |
| `notion_create_page` | Create a new Notion page. |
| `notion_delete_block` | Delete (archive) a Notion block. |
| `notion_duplicate_page` | Duplicate a Notion page — copies properties and content blocks. |
| `notion_get_block_children` | Get the content blocks (children) of a page or block. |
| `notion_get_database` | Retrieve a Notion database's schema and properties. |
| `notion_get_page` | Retrieve a Notion page's properties. |
| `notion_list_comments` | List comments on a Notion page or block. |
| `notion_query_database` | Query a Notion database with optional filters and sorts. |
| `notion_restore_page` | Restore an archived Notion page. |
| `notion_search` | Search Notion pages and databases by title. |
| `notion_update_block` | Update a single Notion block. |
| `notion_update_page` | Update a Notion page's properties. |

### `roost/mcp/tools_okr.py` — 12 tools

| Tool | Description |
|---|---|
| `create_okr_cycle` | Create a new OKR cycle (quarterly period). |
| `create_okr_key_result` | Create a key result under an objective. Max 5 per objective. |
| `create_okr_objective` | Create an objective under a cycle. Max 3 per level per cycle. |
| `get_okr_dashboard` | Full OKR tree with live progress. Defaults to active cycle. |
| `get_okr_scorecard` | End-of-quarter scorecard with achievement bands. |
| `link_task_to_kr` | Link an existing task to a key result. |
| `list_okr_cycles` | List OKR cycles with optional filters. |
| `okr_checkin` | Quick weekly check-in: update a KR's current value and confidence. |
| `unlink_task_from_kr` | Remove a task-KR link. |
| `update_okr_cycle` | Update an OKR cycle. Status follows lifecycle: planning -> active -> scoring -> closed. |
| `update_okr_key_result` | Update a key result's fields, values, confidence, or score. |
| `update_okr_objective` | Update an objective's fields or score it. |

### `roost/mcp/tools_projects.py` — 4 tools

| Tool | Description |
|---|---|
| `create_project` | Create a new project. |
| `get_project` | Get a project with its children and team assignments. |
| `list_projects` | List projects with optional filters. |
| `update_project` | Update an existing project. |

### `roost/mcp/tools_recipes.py` — 17 tools

| Tool | Description |
|---|---|
| `approve_recipe_run` | Approve a run that is awaiting approval and mark it as completed. |
| `classify_inbound_message` | Classify an inbound message using the AI CDR pipeline (standalone). |
| `create_recipe` | Create an automation recipe — a user-defined automation rule. |
| `create_response_template` | Create a canned response template with {{variable}} placeholders. |
| `create_schedule` | Create a scheduled automation from natural language. |
| `delete_recipe` | Delete an automation recipe and its run history. |
| `delete_response_template` | Delete a response template. |
| `execute_recipe` | Execute a recipe against an inbound message using the AI CDR pipeline. |
| `fill_response_template` | Fill a template's {{variable}} placeholders with provided values. |
| `get_recipe` | Get an automation recipe by ID. |
| `get_response_template` | Get a response template by ID. |
| `list_recipe_runs` | List automation run history with optional filters. |
| `list_recipes` | List automation recipes with optional filters. |
| `list_response_templates` | List response templates with optional filters. |
| `skip_recipe_run` | Skip a run that is awaiting approval — do not execute it. |
| `update_recipe` | Update an automation recipe. |
| `update_response_template` | Update a response template. |

### `roost/mcp/tools_scheduled_emails.py` — 3 tools

| Tool | Description |
|---|---|
| `cancel_scheduled_email` | Cancel a pending scheduled email by ID. |
| `list_scheduled_emails` | List scheduled emails in the queue. |
| `schedule_email` | Schedule an email for future sending. |

### `roost/mcp/tools_scrape.py` — 2 tools

| Tool | Description |
|---|---|
| `scrape_js` | Fetch a JS-rendered page via headless browser, optionally run JavaScript. |
| `scrape_url` | Fetch a URL and extract its main content as clean markdown. |

### `roost/mcp/tools_sheets.py` — 5 tools

| Tool | Description |
|---|---|
| `gsheets_append_rows` | Append rows after the last row with data in a Google Sheets range. |
| `gsheets_create_from_template` | Create a new spreadsheet by copying a template and optionally writing data. |
| `gsheets_get_metadata` | Get spreadsheet metadata — title, sheet names, named ranges. |
| `gsheets_read_range` | Read cell values from a Google Sheets range. |
| `gsheets_write_range` | Write values to a Google Sheets range. |

### `roost/mcp/tools_slides.py` — 8 tools

| Tool | Description |
|---|---|
| `gslides_create_from_template` | Create a new Google Slides presentation by copying a template and filling placeholders. |
| `gslides_list_placeholders` | List all slides and text elements in a Google Slides presentation. |
| `gslides_replace_image` | Replace shapes containing placeholder text with an image in a Google Slides presentation. |
| `gslides_replace_text` | Replace placeholder text across all slides in a Google Slides presentation. |
| `pptx_duplicate_slide` | Duplicate a slide within a .pptx file. |
| `pptx_get_slide_notes` | Read speaker notes from all slides in a .pptx file. |
| `pptx_list_placeholders` | List all slides with their shapes, placeholders, and text content from a .pptx file. |
| `pptx_replace_text` | Replace placeholder text across all slides, tables, and notes in a .pptx file. |

### `roost/mcp/tools_ssh.py` — 7 tools

| Tool | Description |
|---|---|
| `list_servers` | List registered servers. |
| `register_server` | Register a remote server for SSH/Docker/K8s management. |
| `remove_server` | Remove a registered server. |
| `scp_download` | Download a file from a remote server via SCP. |
| `scp_upload` | Upload a file to a remote server via SCP. |
| `ssh_exec` | Execute a command on a remote server via SSH. |
| `update_server` | Update a registered server's configuration. |

### `roost/mcp/tools_stats.py` — 4 tools

| Tool | Description |
|---|---|
| `get_completed_task_history` | Get history of completed tasks over a period. |
| `get_daily_completion_counts` | Get daily task completion counts for trend analysis. |
| `get_productivity_summary` | Get a comprehensive productivity summary. |
| `get_weekly_review` | Get a weekly review summary — what was done, what's pending, what's blocked. |

### `roost/mcp/tools_tasks.py` — 4 tools

| Tool | Description |
|---|---|
| `complete_task` | Mark a task as done. |
| `create_task` | Create a new task. |
| `list_tasks` | List tasks with optional filters. |
| `update_task` | Update an existing task's fields. |

### `roost/mcp/tools_tasks_extended.py` — 22 tools

| Tool | Description |
|---|---|
| `add_task_dependency` | Add a dependency: task_id is blocked by depends_on_id. |
| `assign_task` | Assign a contact to a task with a RACI role. |
| `clear_focus` | Remove a task from today's focus, or clear all focus tasks. |
| `get_focus_tasks` | Get today's focused tasks (max 3). |
| `get_next_task` | Get the single most important task to work on right now. |
| `get_task_blockers` | Get all tasks that block a given task (incomplete dependencies). |
| `get_task_progress` | Get progress summary — total tasks, done count, percentage, breakdown by status. |
| `get_today_activity` | Get all activity logged today — actions, tools used, artifacts produced. |
| `list_someday_tasks` | List tasks in the someday pile. |
| `list_subtasks` | List all subtasks of a parent task. |
| `list_task_assignments_mcp` | List task assignments with optional filters. |
| `mark_task_wip` | Start working on a task — sets status to in_progress, stamps last_worked_at, logs activity. |
| `pick_random_task` | Pick a random task weighted by urgency score. |
| `recalculate_task_positions` | Renumber all active task positions 1..N. |
| `remove_task_dependency` | Remove a dependency between two tasks. |
| `reorder_task` | Move a task to a new position in the active list. |
| `set_focus` | Pin a task as today's focus (max 3 at a time). |
| `set_task_last_worked` | Stamp a task with the current time as last_worked_at. |
| `shelve_task` | Move a task to the someday pile (hidden from default views). |
| `suggest_focus` | Suggest top tasks for today's focus when none are pinned. |
| `unassign_task` | Remove a task assignment. |
| `unshelve_task` | Bring a task back from someday to the active list. |

### `roost/mcp/tools_telegram.py` — 2 tools

| Tool | Description |
|---|---|
| `telegram_get_bot_info` | Get info about the configured Telegram bot (verify token works). |
| `telegram_send_message` | Send a Telegram message to authorized users via the bot. |

### `roost/mcp/tools_text_cleanup.py` — 1 tools

| Tool | Description |
|---|---|
| `fix_dashes` | Replace em-dashes and en-dashes with regular hyphens in files. |

### `roost/mcp/tools_time.py` — 5 tools

| Tool | Description |
|---|---|
| `get_running_timer` | Check if a timer is currently running. |
| `get_time_entries` | Get time tracking entries. |
| `get_time_summary` | Get time tracking summary — total hours per task over a period. |
| `start_timer` | Start a time tracking timer for a task. |
| `stop_timer` | Stop the currently running timer. |

### `roost/mcp/tools_wellbeing.py` — 13 tools

| Tool | Description |
|---|---|
| `add_routine_item` | Add an item to a routine, creating the routine if it doesn't exist. |
| `check_shutdown_status` | Check if shutdown mode is currently active. |
| `complete_routine_item` | Mark a routine item as done for today. |
| `execute_resume` | Resume from shutdown — start of day. |
| `execute_shutdown` | Execute the shutdown protocol — end of day wind-down. |
| `get_routine` | Get a routine with its items and today's completion status. |
| `get_spoon_status` | Get current spoon/energy budget status. |
| `get_streak_status` | Get current task completion streak. |
| `remove_routine_item` | Remove an item from a routine. |
| `reset_spoons` | Reset today's spoon count to zero (manual reset). |
| `set_spoon_budget` | Set the daily spoon budget. |
| `spend_spoons` | Manually deduct spoons for an effort level. |
| `uncomplete_routine_item` | Uncheck a routine item for today. |

---

## Bundles (71 tools, 12 modules)

### `roost/extras/crm/mcp/tools_crm.py` — 12 tools

| Tool | Description |
|---|---|
| `crm_append_note` | Attach a note to a person or deal. Returns {note_id} or {error}. |
| `crm_create_deal` | Create a deal/opportunity. |
| `crm_create_person` | Create a person record. `extra` carries vendor-specific attributes. |
| `crm_find_person` | Find the first person in the active CRM by email or phone. |
| `crm_get_person` | Fetch a person by CRM record id. |
| `crm_list_deals` | List deals, optionally filtered by person or stage. |
| `crm_log_communication` | Log a comms event (whatsapp\|telegram\|email\|sms\|call, inbound\|outbound). |
| `crm_move_deal_stage` | Move a deal to a new stage. |
| `crm_search_people` | Free-text search for people in the active CRM. |
| `crm_set_custom_field` | Set a vendor-defined custom attribute on a person or deal. |
| `crm_test_connection` | Ping the active CRM provider. Returns {ok, detail, provider}. |
| `crm_update_person` | Patch a person. `fields` keys may be vendor-specific. |

### `roost/extras/lead_nurture/mcp/tools_lead_pipeline.py` — 3 tools

| Tool | Description |
|---|---|
| `cancel_lead_emails` | Cancel all pending scheduled emails for a lead. |
| `list_pipeline_leads` | List leads in the Practical Cyber pipeline. |
| `update_pipeline_stage` | Update a lead's pipeline stage. |

### `roost/extras/lead_nurture/mcp/tools_leads.py` — 14 tools

| Tool | Description |
|---|---|
| `cadence_delete_preapproval` | Remove a pre-approval rule. Future matching sends will require approval again. |
| `cadence_enroll` | Enroll an existing person in a cadence directly (skips lead_ingest). |
| `cadence_get` | Get a cadence by slug, including steps. Returns 'error' if not found. |
| `cadence_library_status` | List shipped cadence library files and validation state. |
| `cadence_list` | List available nurture cadences. |
| `cadence_list_enrollments` | List enrollments, optionally filtered by status / person / cadence. |
| `cadence_list_preapprovals` | List all pre-approval rules in priority order (most recent first). |
| `cadence_pause` | Pause an active enrollment. No further steps will fire until resumed. |
| `cadence_preapprove` | Create a pre-approval rule. Matching enrollments auto-send instead of |
| `cadence_resume` | Resume a paused enrollment. The next step fires at the next tick if |
| `lead_ingest` | Ingest a lead from any channel into the active CRM + nurture engine. |
| `nurture_approve_pending` | Release a held-for-approval step: send it now, log, and schedule next. |
| `nurture_skip_pending` | Skip the held step without sending. Enrollment advances to the next step. |
| `nurture_tick` | Process every due enrollment once. Normally invoked by the scheduler, |

### `roost/extras/messaging_external/mcp/tools_chatwoot.py` — 1 tools

| Tool | Description |
|---|---|
| `chatwoot_list_templates` | List WhatsApp templates synced to a Chatwoot inbox. |

### `roost/extras/messaging_external/mcp/tools_whatsapp.py` — 4 tools

| Tool | Description |
|---|---|
| `whatsapp_send_document` | Send a PDF / docx / xlsx etc. via WhatsApp. |
| `whatsapp_send_image` | Send a JPG/PNG via WhatsApp. |
| `whatsapp_send_template` | Send a Meta-approved template message (required to start a conversation |
| `whatsapp_send_text` | Send a plain text WhatsApp message. |

### `roost/extras/property_agent/mcp/tools_cdd.py` — 1 tools

| Tool | Description |
|---|---|
| `cdd_screen` | Screen a person against sanctions, PEP, and adverse-media lists. |

### `roost/extras/property_agent/mcp/tools_iras.py` — 3 tools

| Tool | Description |
|---|---|
| `iras_calc_buyer_stamp_duty` | Compute BSD + ABSD on a Singapore property purchase. |
| `iras_calc_lease_stamp_duty` | Compute tenancy / lease stamp duty. |
| `iras_calc_seller_stamp_duty` | Compute SSD on a residential resale. |

### `roost/extras/property_agent/mcp/tools_pdpc.py` — 1 tools

| Tool | Description |
|---|---|
| `pdpc_dnc_check` | Scrub Singapore phone numbers against the PDPC Do-Not-Call registry. |

### `roost/extras/rpa/mcp/tools_rpa.py` — 18 tools

| Tool | Description |
|---|---|
| `rpa_cancel` | Cancel a running or awaiting_input RPA run. |
| `rpa_delete_flow_config` | Delete a stored flow config. |
| `rpa_export_flow` | Write the DB flow config for `portal` to a YAML file. |
| `rpa_get_flow_config` | Return the stored flow config for a portal. |
| `rpa_get_run` | Get the current state of an RPA run, including any pending prompt. |
| `rpa_import_flow` | Read a YAML flow file and write it to the DB, overwriting any existing |
| `rpa_inspect_page` | Load a URL via the chromium sidecar and return a structured summary |
| `rpa_list_flow_configs` | List all flow configs visible to the current user. |
| `rpa_list_flows` | List the portals with a stored flow config (per-user + global). |
| `rpa_list_library` | Inventory the shipped flow YAML files in the library directory. |
| `rpa_list_runs` | List recent RPA runs for the current user. |
| `rpa_run` | Start an RPA flow against a configured portal. |
| `rpa_set_credential` | Store a per-portal credential. Reference inside step `value` as |
| `rpa_set_flow_config` | Create or update a flow config for a portal. |
| `rpa_set_zip_password` | Store a zip password keyed by `(portal, policy_number)`. |
| `rpa_submit_input` | Provide the answer for an awaiting_input RPA run (e.g. an OTP). |
| `rpa_test_step` | Run a single step of a stored flow against the live portal — useful |
| `rpa_validate_flow` | Parse a YAML flow file and return validation errors (empty = valid). |

### `roost/extras/sme_ops/mcp/tools_shopify.py` — 5 tools

| Tool | Description |
|---|---|
| `shopify_cancel_order` | Draft a Shopify order cancellation. Routes through Guardian — the |
| `shopify_fulfill_order` | Mark a Shopify order as fulfilled (all line items). |
| `shopify_list_customers` | List Shopify customers. |
| `shopify_list_orders` | List Shopify orders. `status` accepts open / closed / cancelled / any. |
| `shopify_list_products` | List Shopify products with variant counts and inventory totals. |

### `roost/extras/sme_ops/mcp/tools_stripe.py` — 5 tools

| Tool | Description |
|---|---|
| `stripe_create_payment_link` | Create a one-off Stripe payment link. Returns a shareable URL. |
| `stripe_create_refund` | Draft a Stripe refund. Routes through Guardian — the call is |
| `stripe_list_charges` | List recent Stripe charges (most recent first). |
| `stripe_list_customers` | List Stripe customers. |
| `stripe_list_subscriptions` | List active Stripe subscriptions. |

### `roost/extras/sme_ops/mcp/tools_xero.py` — 4 tools

| Tool | Description |
|---|---|
| `xero_create_invoice` | Create a Xero invoice. |
| `xero_list_bank_transactions` | List Xero bank transactions with reconciliation state. |
| `xero_list_contacts` | List Xero contacts (customers + suppliers). |
| `xero_list_invoices` | List Xero invoices with totals, due dates, and contact references. |
