# Roost — Telegram bot interface
# [job-queue] pulls APScheduler — required by bot/scheduler.py, which drives
# the nurture cadence tick, morning digest, deadline reminders, and urgency
# recalc via app.job_queue. Without it the scheduler disables itself and
# no cadence follow-ups ever send.
python-telegram-bot[job-queue]>=21.0
